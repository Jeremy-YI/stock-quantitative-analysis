[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-indicators"
version = "0.1.0"
description = "A股技术指标计算库（阶段 1：MACD）"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["indicators*"]
