[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-market"
version = "0.1.0"
description = "A股市场业务规则（交易日历 / 涨跌停 / 复权）"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["market*"]
