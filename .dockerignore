# Docker 构建上下文忽略清单：构建上下文是仓库根目录，
# 不忽略的话会把 .venv / node_modules 等几百 MB 的本地产物全塞进上下文。

.git
.gitignore
.dockerignore
.venv
**/__pycache__
**/*.pyc
**/*.egg-info
.pytest_cache
node_modules
.next
out
coverage
*.log

# 环境变量（含密钥，绝不打进镜像）
.env
.env.local

# 数据产物（演示数据 / 概览页快照，运行时现场生成，不打进镜像）
data

# 调度器运行时报告
reports

.DS_Store
.idea
.vscode
