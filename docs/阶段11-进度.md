# 阶段 11：regime 决定选股池 —— 进度（第 2 段完成）

> 状态：第 2 段完成。HEAD 干净，无 remote，不 push。
> 结论详见 `docs/阶段11-报告.md`。一句话：**规则样本外不成立**（大跌切高股息 / 大涨切资金流入均跑输基准，换篮子被单一篮子打败）。

## 第 2 段新增

### 1. 阈值敏感性（主口径改判）
- 脚本 `scripts/run_regime_threshold_sensitivity.py`，落盘 `data/regime_threshold_sensitivity.json`。
- 原 -5% 口径 OOS 大跌仅 4~11 天/年；任务书列 -3%~-6% 无档满足「每段 ≥40」，放宽到 **大跌 ≤ -2%** 才达标（[43,65,78,51]）。
- **主口径：大跌 ≤ -2%、大涨 ≥ +2%**（±3% 为敏感性附录）。

### 2. 板块成分（绕开 AKShare 主接口）
- `sector_stocks.json` 仍找不到；AKShare 主接口 17/29.push2.eastmoney.com 被断连，但延迟镜像 `push2delay.eastmoney.com` 可用、数据格式相同。
- 脚本 `scripts/fetch_sector_members.py` 直接打镜像，串行 + ≥1s 间隔 + 退避重试，拉 33 个东财行业板块成分落库 `data/sector_members.json`（5774 成分，33/33 成功）。

### 3. 两个篮子
- 高股息（242 只）：银行 BK1283(42) + 煤炭 BK0437(34) + 公用事业 BK0427(143，含电力) + 高速公路 BK1483(21) + 福耀 600660 + 格力 000651。Jeremy 18 银行全在 BK1283。
- 资金流入（top-N 板块价量代理）：31 个互斥一级行业（剔除电力⊂公用事业、高速公路⊂交通运输两个二级避免双计），按「20 日相对强度降序 + 成交额占比 20 日变化>0」取 top-5，板块等权。

### 4. 四段回测（核心交付）
- 脚本 `scripts/run_regime_basket_backtest.py`（流式加载 5547 只，绝对/超额分列，确认后滞后 1 日换仓），落盘 `data/regime_basket_backtest.json` + `regime_basket_arrays.npz`。
- 敏感性 `scripts/run_regime_basket_sensitivity.py`，落盘 `data/regime_basket_sensitivity.json`。

**三问答案（全部「否」，且稳健）**：
1. 大跌下高股息跑赢吗？**否**——OOS 汇总 20d 超额 **-2.0pp**（只有 2023 真跌年 +0.4pp）。
2. 大涨下资金流入跑赢吗？**否**——OOS 汇总 20d 超额 **-0.7pp**（N=3/5/8 全负）。
3. 换篮子打败单一篮子吗？**否**——全区间换篮子 +7.3% < 高股息 +23.6% < 全市场 +28.7%，每一段都不是最好。

### 5. 报告与坑
- `docs/阶段11-报告.md`：三坑（前视/幸存者偏差、抗跌≠赚钱分列、regime 滞后 1 日）全部写入；<100 桶标注样本不足。

## 未决 TODO（延续）
- 「牧业」具体标的仍 TODO，未拍。
- 高股息为当前快照近似；point-in-time 股息率需分红事件反推，工作量大，留后续。

## 落盘清单（第 2 段）
- `scripts/run_regime_threshold_sensitivity.py`、`scripts/fetch_sector_members.py`、`scripts/run_regime_basket_backtest.py`、`scripts/run_regime_basket_sensitivity.py`
- `data/`（gitignore，不入库）：regime_threshold_sensitivity.json、sector_members.json、regime_basket_backtest.json、regime_basket_arrays.npz、regime_basket_sensitivity.json
- `docs/阶段11-报告.md`、本文件
