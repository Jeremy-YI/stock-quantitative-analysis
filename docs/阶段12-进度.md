# 阶段 12：pin30 单针下30 分桶 + 仓位状态机 —— 进度

> 状态：任务 1/2/3 完成，任务 4（进攻K叠加）未做。结论详见 `docs/阶段12-报告.md`。
> 一句话：**原始单针（趋势多头+长≥80）20日四段全负；深水单针（非趋势+长≤55）40/60日四段全正；
> 仓位状态机下跌年划算、上涨年是拖累，有效成分是「仓位上限」而非顶背离择时。**

## 新增

### 1. 口径共享层（scripts/pin30_common.py）
- `pin30_series`：把策略里散落的 short/long/st_raw/lt_raw/trend 抽成全序列向量化，抽样核对与
  `strategies.pin30.strategy._evaluate` 逐条一致。
- `bucket_of`：单针下30 四桶口径。

### 2. 任务1 分桶重测（scripts/run_pin30_bucket.py → data/pin30_bucket.json）
- 基础事件=短期随机≤30，四桶 + 深水变体（≤20），四段 × 5/10/20/40/60 持有期，减同期个股基线。
- 结果：桶1（原始）20日 4/4 负；桶3（深水）40/60日 4/4 正；深水变体略强于桶3。

### 3. 背离模块（packages/indicators/indicators/divergence/）
- `swing_lows`（与 double_bottom 逐行一致）+ `swing_highs`（对称）。
- `detect_bullish_divergences`（double_bottom 口径：DIF/柱/RSI 三取二、DIF 硬条件）。
- `detect_bearish_divergences`（顶背离：pivot high 价新高/齐平但 DIF 走低），
  已确认时点=右 pivot + k 根（无前视），在 159828 的 8/20 案例上复现。

### 4. 任务2 仓位状态机（scripts/run_pin30_state_machine.py → data/pin30_state_machine.json）
- 逐日状态机：顶背离→降到底仓（分步/一次），单针→加仓（+30pp/×1.3，封顶 70%/100%）。
- 对比买入持有满仓 / 固定50% / 状态机，指标：收益/回撤/夏普/卡玛/平均仓位/换手。
- 24 参数网格全落盘。

### 5. 任务3 信号明细（scripts/run_pin30_cases.py → data/pin30_cases.json）
- 三只点名股（600206/600522/601398）+ 医药生物（BK1216，509 只）逐条分桶标签 + 加仓点位 +
  前向 5~60 日收益。

## 未决 TODO
- 任务4（进攻K叠加 pin30）：需先实现 attack_candle_level（specs/背离定价线指标规格.md 指标3）。
- 桶3 叠加「底背离+增量资金+不破底」三道前置的分桶重测（add_deep 消融）。
- 「长线资金拐头向下」资金指标名待 Jeremy 确认。

## 落盘清单
- `scripts/pin30_common.py`、`scripts/run_pin30_bucket.py`、`scripts/run_pin30_state_machine.py`、`scripts/run_pin30_cases.py`
- `packages/indicators/indicators/divergence/`（divergence.py + __init__.py）
- `data/`（gitignore）：pin30_bucket.json、pin30_state_machine.json、pin30_cases.json
- `docs/阶段12-报告.md`、本文件
