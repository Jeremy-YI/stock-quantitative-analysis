# 阶段 13：区间化背离 + 双防线入场/止损体系 —— 进度

> 状态：三步全部完成。结论详见 `docs/阶段13-报告.md`。
> 一句话：**159828 案例 k=3 全复现；「站上生命线」修好了过度止损的机制（5日破线 87~95%→28~35%）
> 但不产生收益——深水单针(探底当天)25/60日超额四段仍全正；第二道进攻K中点在个股≈不触发；
> 长上影线卖出是纯防守（MAE砍6~14pp、牛市收益砍半）。**

## 新增

### 1. 区间化背离 + 双防线模块（packages/indicators/indicators/stage13.py）
- `detect_bottom_divergences`：摆动低点 + 收盘创新低（不破底用收盘价）+ 紧邻顶背离。
- `detect_top_divergences`：摆动高点（区间顶）。
- `lifeline_value`：生命线 = (顶背离区间最高 + 底背离区间最低)/2。
- `attack_midpoint` = (开+收)/2；`detect_attack_candles`（main≥10%/star≥20%/etf≥4%/bse排除）。
- `attack_stack` / `current_attack_midpoint`：进攻 K 栈逻辑（逐日模拟，破了出栈回退）。
- `upper_shadow_ratio` / `is_long_upper_shadow`：长上影线判定。

### 2. 159828 案例回归单测（tests/unit/test_stage13.py，12 项全过）
复现 Jeremy 全部标注：生命线 4.055/3.880、进攻 K 3.775/4.210、7/06 上影 61.5%、
7/07 破/7/10 站回、8/24 单针 7.1/49.0 未破 4.21、增量资金绿柱收缩。

### 3. 全市场四段回测（scripts/run_stage13.py → data/stage13_backtest.json）
- 新口径四步入场（底背离→站上生命线→单针→买入）vs 旧口径探底当天（深水单针）。
- 双防线持有（生命线减仓/洗盘 + 进攻K中点真正离场）+ 固定 5/13/25/60 参照。
- 长上影线卖出（阈值 50/60/70% × 卖 50/70/100%）。
- 关键数：5日破线 旧87~95% vs 新28~35%；新口径收益四段均≤旧；被止损(进攻K)≈0%；
  长上影线卖100% OOS-C +8.47%→+0.85%（MAE -9.9%→-3.4%）。

## 未决 TODO
- 双防线未做「减仓 50%」的仓位级现金流记账（现只标记破线/洗盘事件，收益按满仓到离场）。
- 进攻 K 阈值自适应（涨幅 ≥ 3×近20日平均振幅，规格脚注建议）——现用硬阈值 10%/20%/4%。
- 若 Jeremy 坚持「顶背离」必须是 MACD 顶背离（而非摆动高点），生命线锚点会变，需回炉核对。

## 落盘清单
- `packages/indicators/indicators/stage13.py`、`tests/unit/test_stage13.py`
- `scripts/run_stage13.py`、`data/stage13_backtest.json`（gitignore）
- `docs/阶段13-报告.md`、本文件
