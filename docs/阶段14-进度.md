# 阶段 14：架构重构——MACD 柱状阶段顶底进出场 + 三条线降级为洗盘判别器 —— 进度

> 状态：四步全部完成。结论详见 `docs/阶段14-报告.md`。
> 一句话：**柱状阶段顶底原语 159828 全复现（8 项单测）；但「单针+阶段底」加确认仍买贵、
> 完整架构仍纯防守；最核心发现是三条线当洗盘判别器方向反了——单针时「回踩不破」后续收益
> 系统性低于「已破」（四条线口径四段几乎全负），进攻K在个股上空转。**

## 新增

### 1. 柱状阶段顶底 + 2日容错 + 三条线序列模块（packages/indicators/indicators/stage14.py）
- `detect_hist_stage_bottoms` / `detect_hist_stage_tops`：柱状极值转折（N=5/10/20 可测）。
- `line_broken_2day`（破位容错：连续2日跌破才算破）/ `line_broken_1day`（单日对照）。
- `lifeline_series`（复用 stage13 k=3 摆动点口径）、`yin_volume_line_series`（区间量最大阴线最高价 V2P1）、
  `attack_defense_series`（进攻K中点 + 跳空开盘改用跳空K线最低价，栈逻辑）。

### 2. 159828 案例回归单测（tests/unit/test_stage14.py，8 项全过）
复现 Jeremy 全部标注：阶段底 3/25、阶段顶 7/06（叠加上影 61.5%）、绿柱底背离买点 7/29、
8/24 单针未破 4.21 洗盘、7/07 破位容错（单日不算破/7/08 连续2日算破）。

### 3. 全市场四段回测（scripts/run_stage14.py → data/stage14_backtest.json，gitignore）
- 三组对照：纯单针(深水) / 单针+阶段底 / 完整架构（固定 25/60 持有 + 仓位循环）。
- 三条线判别器价值检验（单针事件分洗盘/破位，比 5/13/25/60 日前向收益差）+ 2日容错 vs 单日对照。
- 3-2-2-1 仓位循环（首笔3成 + 洗盘加仓2/2/1成 + 阶段顶卖50%），仓位级现金流记账，对照满仓/固定50%。
- 关键数：
  - 组1 vs 组2 超额：25日 IS +0.76 vs +0.39 / OOS-A +0.62 vs +0.33 / OOS-C +1.59 vs +0.45（加确认全更差）。
  - 完整架构 vs 满仓：IS -0.87 vs -10.55 / OOS-C +4.76 vs +53.56（防守救命、进攻跑输49pp）。
  - 判别器 25日 Δ（组A洗盘−组B破位）：生命线 -1.67/-1.26/-2.57/-2.00；阴量线 -1.72/-0.89/-2.09/-1.81；
    任一不破 -2.16/-1.53/-2.85/-2.13；全部不破 -1.67/-0.95/-1.97/-1.85（四段全负 = 方向反了）。
  - 进攻K防线 B组（已破2日）n=0（个股空转，与阶段13一致）。

## 未决 TODO
- 进攻 K 阈值自适应（涨幅 ≥ 3×近20日平均振幅）——现用硬阈值 10%/20%/4%，个股上依旧几乎不触发。
- 判别器结论是负 alpha（方向反），若要可用需「反向用」（已破=更超卖=买），但这已脱离 Jeremy 原口径，
  未擅自改，留待 Jeremy 裁决。
- 仓位循环未计交易成本/涨跌停约束，未做停牌处理（与阶段 13 同一简化）。

## 落盘清单
- `packages/indicators/indicators/stage14.py`、`tests/unit/test_stage14.py`
- `scripts/run_stage14.py`、`data/stage14_backtest.json`（gitignore）
- `docs/阶段14-报告.md`、本文件
